#include "Helpers.h"

std::list<Instruction>::iterator findReadAccessRegister(std::list<Instruction>& list,
	std::list<Instruction>::iterator start, 
	zasm::Reg& searchedRegister)
{
	return std::list<Instruction>::iterator();
}
